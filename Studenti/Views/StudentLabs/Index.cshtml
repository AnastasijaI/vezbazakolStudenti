@model IEnumerable<Studenti.Models.StudentLab>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" >Create New</a>
</p>@* asp-route-id="@Model.FirstOrDefault()?.LabVezbaId" *@
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Student)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LabVezba)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Zavrsena)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Poeni)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataZavrsuvanje)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Student.ImePrezime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LabVezba.Naslov)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Zavrsena)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Poeni)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataZavrsuvanje)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
